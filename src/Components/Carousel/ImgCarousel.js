import React from 'react';

const ImgCarousel = () => {
  return (
    <div style={styles.mainWrapper}>
      lol
    </div>
  )
};

const styles = {
  mainWrapper: {
    backgroundColor: 'red',
    minHeight: '20vh',
    minWidth: '100%',
    marginTop: 30
  }
};

export default ImgCarousel;